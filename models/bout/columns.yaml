# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    cache_name:
        label: 'Name'
        type: text
        searchable: true
    fencersWrongWayWarning:
        label: 'Fencers Wrong Way'
    left_score:
        label: 'Left Score'
        type: number
    right_score:
        label: 'Right Score'
        type: number
    video_url:
        label: Video Url
    actions:
        label: Actions
        relation: actions
        useRelationCount: true
