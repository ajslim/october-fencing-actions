# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    cache_name:
        label: 'Name'
        type: text
        searchable: true
    left_score:
        label: 'Left Score'
        type: number
    right_score:
        label: 'Right Score'
        type: number
    actions:
        label: Actions
        relation: actions
        select: video_url